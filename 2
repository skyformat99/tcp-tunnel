#ifndef TCPTUNNEL_PROTOCOL_H
#define TCPTUNNEL_PROTOCOL_H

#define _GNU_SOURCE
#include "netutils.h"
#undef _GNU_SOURCE

typedef struct {
    uint8_t  type;
    ipaddr4_t addr;
    portno_t  port;
} __attribute__((packed)) packet_connect4;

#endif
